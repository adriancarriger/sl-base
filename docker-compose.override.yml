version: "3.5"
services:
  server:
    volumes:
      - ../sl-server:/app
      - /app/node_modules
  data:
    volumes:
      - ../sl-data:/app
      - /app/node_modules
  lambda:
      volumes:
        - ../sl-lambda:/app
        - /app/node_modules
